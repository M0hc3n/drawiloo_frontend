import 'package:flutter/material.dart';

class RoomMode extends StatelessWidget {
  const RoomMode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
